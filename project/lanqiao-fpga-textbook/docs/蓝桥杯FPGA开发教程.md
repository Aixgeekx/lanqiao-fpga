# 蓝桥杯 FPGA 开发教程 Markdown 摘要

作者：Aix，极道工作室

## 教材定位

这本教程面向零基础读者，同时按赛场纸质速查手册和未来出版稿的标准迭代。目标不是只给出能跑的代码，而是解释每个模块为什么这样写、硬件为什么这样接、竞赛题目为什么要拆成这些状态机和数据通路。

## 学习主线

1. 先认识 CT137X 竞赛平台：时钟、复位、按键、LED、数码管、串口、I2C 外设、RTC、SRAM 和 Flash。
2. 再掌握 XDC 约束：每个顶层端口必须绑定到真实 FPGA 管脚，并设置 `LVCMOS33`。
3. 接着完成 Vivado 工程创建流程：新建 RTL 工程、添加源码、绑定约束、综合实现、生成 bitstream、下载验证。
4. 然后学习基础驱动：按键消抖、动态扫描、UART、I2C、SPI、SRAM、DS1302、W25Q128。
5. 最后进入竞赛题：把题目拆成数据采集、存储、算法计算、界面显示和串口上报。

## 当前 PDF 章节

- 第一章：硬件平台概览
- 第二章：引脚约束与配置
- 第三章：核心原理与真题实战
- 第四章：Driver 驱动模块详解
- 第五章：User 应用模块详解
- 附录：DS1302 与 W25Q128 参考表格、FPGA术语速查、常见Verilog错误与修正

新版 PDF 在正文前加入了出版说明、纸质使用建议、零基础14天学习路线、赛场速查卡、赛场上板调试清单、Verilog 综合规则与赛场写法清单和 50MHz 计数参数与定时器写法速查，封面署名为 Aix，极道工作室。全部31个表格带"表 N"自动编号，46个题面页带"图 N"自动编号，附录含12条参考文献。

赛场上板调试清单按“综合前、约束、实现、下载、基础IO、外设、综合题”七步排查，并给出LED全灭、数码管乱码、按键跳多次、I2C无ACK、UART乱码、SPI读回全FF/全00、板上偶发死机等现象的定位动作。

Verilog 综合规则页把 `wire`、`reg`、`assign`、`always @(posedge clk)`、`always @(*)`、阻塞赋值和非阻塞赋值映射到实际硬件含义，并列出 multiple drivers、latch inferred、端口未连接、位宽截断等常见报错的修正动作。

50MHz 计数参数页把 1us、1ms、20ms 消抖、1s 倒计时、UART 115200、1kHz 数码管扫描和 1kHz PWM 90%/10% 占空比换算成可直接照抄的计数值，并说明计数器通常从 0 数到 N-1。

第三章已把第16届国赛整理为纸质手册式结构摘要，覆盖硬件资源、性能要求、数据录入、算法、显示、按键功能、串口上报和推荐顶层状态划分。

第三章还加入了真题训练索引、真题模拟题训练顺序、训练记录栏与错题复盘模板。纸质训练时可以先按索引定位题组对应的知识点，再按限时顺序训练，把错误原因归类为概念不清、时序错误、极性错误或接口混淆，最后回到对应章节修正。

## 重点知识点

### I2C

I2C 使用两根线：`SCL` 提供时钟，`SDA` 传输数据。总线空闲时两根线都被上拉为高电平。起始条件是 `SCL` 为高时 `SDA` 从高变低，停止条件是 `SCL` 为高时 `SDA` 从低变高。每个字节后面跟一个应答位，接收方把 `SDA` 拉低表示 ACK。

### UART

UART 不需要时钟线，双方靠约定波特率同步。常见 8N1 帧格式为 1 位起始位、8 位数据位、无校验、1 位停止位。50 MHz 时钟下 115200 波特率的计数值约为 `50_000_000 / 115200 = 434`。

### SPI

SPI 由主机发起通信，常用信号包括 `CS`、`SCLK`、`MOSI`、`MISO`。CPOL 决定空闲时钟电平，CPHA 决定采样边沿。标准 SPI 可全双工发送和接收，DS1302 属于类似 SPI 的三线协议，需要额外处理位序和单数据线三态控制。

### 状态机

复杂外设控制通常用状态机描述。三段式状态机把状态寄存器、下一状态组合逻辑、输出逻辑分开，便于综合和调试。Moore 状态机输出只由状态决定，Mealy 状态机输出由状态和输入共同决定。

### 按键消抖

机械按键按下或释放时会产生几毫秒到十几毫秒的抖动。工程上常用 10 ms 或 20 ms 低频采样，只在连续稳定后产生单周期脉冲，避免一次按键被识别成多次。

### 数码管动态扫描

多位数码管通常共用段选线，依次打开每一位的位选。只要扫描频率足够高，人眼由于视觉暂留会看到所有位同时点亮。8 位数码管常用 1 kHz 左右扫描，每位刷新约 125 Hz。

### 板载存储与时钟器件

SRAM 是异步并口存储器，读写时要满足地址建立、访问和保持时间。DS1302 使用三线类 SPI 协议，时间寄存器为 BCD 格式，并支持突发读写。W25Q128 是 SPI NOR Flash，写入前必须写使能，页编程和扇区擦除后必须轮询 WIP 状态位。

## 已纳入题面

新版 PDF 已嵌入以下扫描题面，共 46 页：

- 第十六届 蓝桥杯（电子类）FPGA设计与开发省赛真题
- 第十七届蓝桥杯FPGA省赛真题（客观题）
- 第十七届蓝桥杯FPGA省赛真题（设计题）
- 十六届蓝桥杯FPGA模拟试题I
- 十六届蓝桥杯FPGA模拟试题Ⅱ
- 十六届蓝桥杯FPGA模拟试题Ⅲ
- 第十七届FPGA模拟考试Ⅰ
- 第十七届FPGA模拟考试Ⅱ
- 第十七届FPGA模拟考试Ⅲ

## 发布文件

- PDF 成品：`pdf/蓝桥杯FPGA开发教程_详细注释版.pdf`
- 生成脚本：`scripts/generate_textbook_v3.py`
- 板卡资料索引：`docs/title_docs_extract.md`
- 项目阅读总结：`docs/project_reading_summary.md`

当前 PDF 为 117 页，约 7.0 MB。脚本会把 46 页扫描题面图片压缩缓存为 JPEG 后再嵌入 PDF，避免大量原始 PNG 在两遍生成流程中造成内存压力。

## 附录新增内容

附录新增 FPGA 术语速查表，涵盖 CLB、LUT、FF、BRAM、CMT、IOB、综合、布局布线、比特流、异步/同步复位、亚稳态、两级同步器、状态机、阻塞/非阻塞赋值等 17 个核心术语。

附录还新增常见 Verilog 错误与修正表，列出时序块用阻塞赋值、组合块用非阻塞赋值、assign 驱动 reg、异步复位未加入敏感列表、case 缺少 default、for 循环在 always 外未用 generate 等 8 个典型错误及其正确写法。

## 官方资料依据

硬件事实优先来自 `title` 目录中的官方资料：`CT137X_UM.pdf`、`CT137X_PIN.pdf/xlsx`、`CT137X_SCH.pdf`、`SEG_TABLE.pdf` 以及 ADC、DAC、AT24C02、DS1302、IS63WV1288、W25Q128 等芯片数据手册。PDF 附录已加入官方资料依据速查表和 CT137X 关键硬件事实表。

## 后续迭代

后续会把 PDF 中的完整章节逐步同步为 Markdown 正文，并补全全部真题模拟题的文字化解析。
